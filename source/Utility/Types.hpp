#pragma once

typedef size_t MeshID; // Unique ID of a mesh, represents the index of the Mesh in the Mesh container.
typedef size_t TextureID; // Unique ID of a texture, represents the index of the Texture in the Texture container.
const size_t MAX_TEXTURES = 50;